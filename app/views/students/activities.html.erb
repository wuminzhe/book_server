<% content_for :title do " - #{@student.name}的活动" end %>

<div id="nav_title" class="row">
  <div class="col-md-6">
    <ol id="path" class="breadcrumb">
      <li><a href="<%= url_for klass_students_path(@student.klass) %>">所有学生</a></li>
      <li class="active"><%= @student.name %></li>
    </ol>
  </div>
  <div id="actions" class="col-md-6">
  </div>
</div>

<div id="main">
  <%= render partial: 'activities', object: @student %>
</div>
